<Project>
<ItemGroup>
    <!-- NOTE; Expose internal types to the library sharing the same name, but suffixed with '.Tests' -->
    <AssemblyAttribute Include="System.Runtime.CompilerServices.InternalsVisibleTo">
      <_Parameter1>$(MSBuildProjectName).Tests</_Parameter1>
    </AssemblyAttribute>
  </ItemGroup>
</Project>